name: ciomart_server
description: CIOMART REST API Server
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  mysql_client: ^0.0.27
  crypto: ^3.0.3
  args: ^2.4.2

dev_dependencies:
  lints: ^3.0.0
